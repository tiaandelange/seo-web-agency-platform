# Copy to .env.local for development. Never commit real values.

# Absolute site origin, no trailing slash. Drives canonicals, sitemap, robots, JSON-LD URLs.
# Keep localhost for local work. Use the preview host for private previews.
# Do NOT set https://koppiesystems.co.za until domain ownership is confirmed.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# "production" enables indexing (robots allow + sitemap). Anything else = staging: robots disallow all.
# Private previews must remain non-production.
NEXT_PUBLIC_SITE_ENV=development

# Optional: GA4 measurement ID. Analytics stays disabled while unset.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Optional: Google Search Console HTML-tag verification content value.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Optional: where lead-form submissions are POSTed (email service / automation webhook).
# While unset, submissions are logged server-side only. See lib/actions.ts and docs/technical/FORM-ARCHITECTURE.md.
LEAD_WEBHOOK_URL=
